buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.1.4") // O la versión estable más reciente
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0") // O la versión estable más reciente de Kotlin
    }
}

allprojects {
    repositories {
    
    }
}